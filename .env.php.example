<?php
/**
 * tokps configuration template.
 * Copy this file to .env.php (same directory) and adjust the values.
 */
return [
    // Absolute path to your opencode database.
    //   Linux:   /home/YOU/.local/share/opencode/opencode.db
    //   macOS:   /Users/YOU/.local/share/opencode/opencode.db
    'db' => '/home/YOU/.local/share/opencode/opencode.db',

    // Directory for the HTML page cache (created automatically; must be
    // writable by the PHP user). Default: inside the app directory.
    // On publicly served hosts, prefer a path OUTSIDE the docroot.
    'cache_dir' => __DIR__ . '/cache',

    // Page cache TTL in seconds. The SQLite database is only queried
    // once per TTL; every other request is served a static HTML file.
    'ttl' => 300,
];
